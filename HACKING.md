# normfn - Hacking

To get started, run:

*   `uv sync --locked --all-extras --dev` (if using UV package manager)
*   OR `pip3 install freezegun pexpect` (if using pip)

See `.github/copilot-instructions.md` for complete development setup and guidelines.
